# A more advanced idle and lock setup
exec swayidle -w \
    timeout 900 'swaylock -f' \
    timeout 1200 'systemctl suspend' \
    before-sleep 'swaylock -f' \
    lock 'swaylock -f' \
    resume 'swaymsg "output * dpms on"; swaymsg "input * enabled 1"'
